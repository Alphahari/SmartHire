apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv
  namespace: smarthire
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/db
  storageClassName: ""